//StringBuffer  --> it is mutable string object we can change string data 
//by using stringbuffer 

//stringbuilder contain all the method of StringBuffer methods but only one difference is 
//stringbuilder support multithreading while stringbuffer not support multithreading 
// only one thread(process) is performed in stringbuffer . so for this reason stringbuffer 
// is less effective than stringbuilder.


import java.util.Scanner;
public class StringBuffer_methods {
    public static void main(String[] args) {
    Scanner sc = new Scanner(System.in);
    String s = sc.nextLine();
    StringBuffer str = new StringBuffer(s);

    //1. public int length():-This method return the no of characters 
    //present in the StringBuffer. 
    //System.out.println("length = "str.length());


    //2. public int capacity():-This method returns the total no of 
    //characters that can hold StringBuffer object.
    //System.out.println(str.capacity());


    //3. public char charAt(int index):-This method returns the character 
    // located at a specified index. 
    //System.out.println(str.charAt(2));


    //4. public void setCharAt(int index, char ch):-This method replace 
    // the character located at the specified index with the provided character.
    // str.setCharAt(3,'h');
    // System.out.println(str);


    // 5. public StringBuffer insert(int index,String str):-To insert at 
    // the specified location. 
    // str.insert(3,'h');
    // System.out.println(str);


    //6. public StringBuffer append(String str):-By using this append 
    //string value,int value , long value and other type value in string.
    // str.append(123);
    // System.out.println(str);


    //7. public StringBuffer reverse();
    // str.reverse();
    // System.out.println(str);


    // 8. public void trimToSize():-To deallocate the extra allocated free 
    // memory such that capacity and size are equal.
    // str.trimToSize();
    // System.out.println(str.capacity());


    // 9. public void ensureCapacity(int initialcapacity):-To increase 
    // the capacity dynamically(fly) based on our requirement. 
    str.ensureCapacity(5);
    System.out.println(str.capacity());


    sc.close();
   }
}
