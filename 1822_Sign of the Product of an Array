//problem-1822(LEETCODE).
//TIME-COMPLEXITY --> O(n).


class Solution {
    public int arraySign(int[] nums) {
        int n = 0;
        for(int i : nums){
            if(i==0) return 0;
            if(i<0) n++;}
        if(n%2==0) return 1;
        return -1;
    }
}
